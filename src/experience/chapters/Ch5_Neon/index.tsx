import { GreyboxBiome } from '../GreyboxBiome'
import { neonConfig } from './config'

/** Biome root — greybox until its Phase 4 art pass. */
export default function Ch5_Neon() {
  return <GreyboxBiome zone={5} config={neonConfig} />
}
